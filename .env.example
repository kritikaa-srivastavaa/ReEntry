# Public build-time setting, never a secret. Rebuild after changing.
VITE_API_URL=http://localhost:3001/api
# Set 90000 for the temporary free hosted demo to allow server cold starts.
VITE_API_TIMEOUT_MS=20000
